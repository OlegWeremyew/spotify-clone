import {FC} from "react";

export const TheButtonLogin: FC = () => {
  return (
    <button
      type="button"
      className="bg-white text-[#2e2e2e] text-xs font-semibold leading-5 tracking-widest uppercase py-[9px] px-[17px] sm:px-[38px] rounded-full hover:scale-105"
    >
      Log In
    </button>
  );
}

